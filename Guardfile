guard :sass, input: "sass", output: "public/css"

guard :livereload do
  watch %r{views/.+\.haml}
  watch %r{views/css/main\.css}
end

# guard :haml, input: ".", output: [".", "en"] do
#   watch %r{.+\.haml}
# end