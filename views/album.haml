-# todo: move album class in body class

%a.btn{ href: "/" }= t "< back", "< indietro"

.album
  %h1= @album.name

  .album
    - @album.photos.each do |photo|
      - img = photo.images[4]
      %img{ src: img["source"] }