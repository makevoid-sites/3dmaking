-# todo: move album class in body class

%a.btn{ href: "/photos" }
  <
  = t "all photos", "tutte le foto"

.album.box
  %h1= @album.name
  .boxed
    .album
      - @album.photos.each do |photo|
        -# TODO: choose photo sizes for display and lightbox, not sure wich is wich, developed offline
        - img = photo.images[4]
        - big = photo.images[1]
        %a.alb{ href: img["source"], data: { :"fancybox-group" => "group" } }
          %img{ src: img["source"], data: { big: big["source"] } }
